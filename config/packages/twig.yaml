twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        category_service: '@App\Repository\CategoryRepository'
    form_themes:
        - 'bootstrap_3_layout.html.twig'
